# `skills` has been a gitlink since it was first added — the tree records a
# commit of markdown-viewer/skills — but without this file nothing said where
# to fetch it from, so a fresh clone got an empty directory and
# `git submodule update --init` had nothing to work with.
