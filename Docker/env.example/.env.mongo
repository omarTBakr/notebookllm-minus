# Template for Docker/env/.env.mongo. Read by `docker compose` itself
# (--env-file), substituted into docker-compose.yml; not passed to a container.
#
# Only used with `docker compose --profile mongo`. Must match MONGO_USER /
# MONGO_PASSWORD in .env.app.
MONGO_INITDB_ROOT_USERNAME="root"
MONGO_INITDB_ROOT_PASSWORD="REPLACE_ME"
