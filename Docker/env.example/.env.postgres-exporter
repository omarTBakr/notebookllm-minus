# postgres_exporter takes its connection string from DATA_SOURCE_NAME, which
# docker-compose.yml builds from the POSTGRES_* values in .env.postgres. Nothing
# is needed here unless you want to override it or add custom queries.
#
# DATA_SOURCE_NAME="postgresql://user:pass@pgvector:5432/db?sslmode=disable"
