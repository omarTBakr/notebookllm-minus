# Template for Docker/env/.env.nginx. Read by `docker compose` itself
# (--env-file) and substituted into docker-compose.yml; not passed to a container.
#
# Host port for the nginx reverse proxy. Pick one that is free on your machine;
# the app is also reachable directly on 8000 without nginx at all.
NGINX_PORT="8080"
